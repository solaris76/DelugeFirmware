# Phi-Family Zone (PhiMorph / PhiStair / PhiWeave / PhiVox / PhiSwarm / PhiGendy)

Selects the zone for whichever phi-family oscillator is active (the same two menu slots serve all three). For PhiMorph, each zone position produces a distinct waveform shape via phi-triangle evaluation; for PhiWeave, it selects the mass-spring ring's physical terrain (stiffness/damping landscapes, home shape, bowing — see docs/dev/phi-weave-physics.md); for PhiVox, it selects a voice character (formant pair, pulse counts, decay, breath — see docs/dev/phi-vox-vosim.md); for PhiSwarm, it selects a synchronization regime (slave detune ratios, coupling, temperature — see docs/dev/phi-swarm-sync.md); for PhiGendy, it selects stochastic terrain (entropy, elastic barriers, home shape — see docs/dev/phi-gendy-stochastic.md); for PhiStair, it selects the staircase architecture (height pattern family, count, asymmetry, slope — see docs/dev/phi-stair-steps.md).

Available when oscillator type is any phi-family oscillator.
